<!DOCTYPE html>
<html>
	<%- include('header') %>
	<body>
		<h1>New post</h1>
		<form method="POST">
			<p>
				<label for="nickname">Your Name</label>
				<input
					id="nickname"
					name="nickname"
					value="<%- values.nickname ? sanitize(values.nickname) : 'Anonymous' %>"
				/>
			</p>
			<% if (errors.nickname) { %>
			<div class="error"><%- errors.nickname %></div>
			<% } %>

			<p>
				<label for="message">Text</label>
				<textarea id="message" class="medium" name="message">
<%- values.message ? sanitize(values.message) : '' %></textarea
				>
			</p>
			<% if (errors.message) { %>
			<div class="error"><%- errors.message %></div>
			<% } %>

			<button class="right-button">Send</button>
		</form>
		<p></p>
		<form class="buttonForm" action="/showPosts" method="GET">
			<button type="submit">Show Posts</button>
		</form>
	</body>
</html>
